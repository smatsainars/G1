; Don't show PHP errors in the browser - they reveal file paths and code.
display_errors = Off

; Log them instead. See them with: docker compose logs app
log_errors = On

; Don't tell visitors which PHP version runs the site (X-Powered-By header)
expose_php = Off